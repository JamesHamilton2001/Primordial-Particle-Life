// #ifndef SPATIALHASH_HPP
// #define SPATIALHASH_HPP

// #include <raylib.h>

// #include <vector>


// class SpatialHash
// {
//  public:

//     SpatialHash(int size);
//     ~SpatialHash();

//     void mapGrid(std::vector<Vector2>& positions);

//     inline std::vector<int>& SpatialHash::getCell(int r, int c) { return gridIds[r][c]; }
    
//  private:

//     const int size;

//     std::vector<std::vector<std::vector<int>>> gridIds;
//     std::vector<std::vector<int>> gridCounts;

// };


// #endif